import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  flatListContainer: {
    backgroundColor: '#fff',
    marginHorizontal: 10,
    marginVertical: 10,
    borderRadius: 10
  }
});
